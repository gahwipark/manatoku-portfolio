<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="dao.FriendsMapper">

  <select id="getFriendsList" resultType="model.Friends">
  	SELECT f.ucode,f.friend_ucode,m.id AS friend_id,m.name AS friend_name, f.stat as friend_stat
  	FROM friends f JOIN member m
  	ON f.friend_ucode=m.ucode
  	WHERE f.ucode=#{ucode}
  </select>

</mapper>